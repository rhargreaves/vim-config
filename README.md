# Vim Config

## Installation

```bash
$ git clone git@github.com:rhargreaves/vim-config.git
```

### Vim

```bash
$ ln -s $(pwd)/vim-config/vim ~/.vim
$ ln -s ~/.vim/vimrc ~/.vimrc
```

### Neovim

```bash
$ mkdir -p ~/.config
$ ln -s $(pwd)/vim-config/nvim ~/.config/nvim
```
